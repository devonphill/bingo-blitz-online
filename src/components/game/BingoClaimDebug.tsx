
import React from 'react';

interface BingoClaimDebugProps {
  // Props if needed
}

// Empty component that doesn't render any UI - all debugging now happens via console
export default function BingoClaimDebug({}: BingoClaimDebugProps) {
  return null;
}

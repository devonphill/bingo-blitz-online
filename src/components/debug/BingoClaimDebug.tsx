
import React from 'react';

interface BingoClaimDebugProps {
  // Props if needed
}

// Empty component that doesn't render any UI
export default function BingoClaimDebug({}: BingoClaimDebugProps) {
  return null; // Return nothing - debug functionality now lives in claimDebugUtils.ts
}
